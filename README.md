# streamlit-ranking-shapley

Very basic implementation of Streamlit to show a prediction dataframe + shapley values for each row clicked.

# Running

1. `poetry install`

2. `poetry run streamlit run rec.py`
